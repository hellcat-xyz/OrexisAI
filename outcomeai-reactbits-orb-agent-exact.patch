diff --git a/.gitignore b/.gitignore
index 56a0626..2c6b4fe 100644
--- a/.gitignore
+++ b/.gitignore
@@ -5,3 +5,5 @@ npm-debug.log*
 .reactbits/
 public/hyperspeed.js
 public/hyperspeed.css
+public/orb.js
+public/orb.css
diff --git a/package-lock.json b/package-lock.json
index b6189ca..fa85bd1 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -10,6 +10,7 @@
       "hasInstallScript": true,
       "dependencies": {
         "bcrypt": "^6.0.0",
+        "ogl": "^1.0.11",
         "pg": "^8.16.3",
         "postprocessing": "^6.36.0",
         "react": "^19.0.0",
@@ -541,6 +542,11 @@
         "node-gyp-build-test": "build-test.js"
       }
     },
+    "node_modules/ogl": {
+      "version": "1.0.11",
+      "resolved": "https://registry.npmjs.org/ogl/-/ogl-1.0.11.tgz",
+      "license": "Unlicense"
+    },
     "node_modules/pg": {
       "version": "8.22.0",
       "resolved": "https://registry.npmjs.org/pg/-/pg-8.22.0.tgz",
diff --git a/package.json b/package.json
index 4437f8f..ec36d33 100644
--- a/package.json
+++ b/package.json
@@ -7,13 +7,16 @@
   "scripts": {
     "start": "node server.js",
     "dev": "node --watch server.js",
-    "check": "node --check server.js && node --check database.js && node --check plans.js && node --check payment-service.js && node --check gemini-service.js && node --check public/app.js && node --check scripts/build-reactbits-hyperspeed.mjs && node --check public/login.js && node --check public/register.js && node --check views/login.js && node --check views/register.js && node --check views/dashboard.js",
+    "check": "node --check server.js && node --check database.js && node --check plans.js && node --check payment-service.js && node --check gemini-service.js && node --check public/app.js && node --check scripts/build-reactbits-hyperspeed.mjs && node --check scripts/build-reactbits-orb.mjs && node --check public/login.js && node --check public/register.js && node --check views/login.js && node --check views/register.js && node --check views/dashboard.js",
     "test": "node --test",
     "build:hyperspeed": "node scripts/build-reactbits-hyperspeed.mjs",
-    "postinstall": "npm run build:hyperspeed"
+    "build:orb": "node scripts/build-reactbits-orb.mjs",
+    "build:reactbits": "npm run build:hyperspeed && npm run build:orb",
+    "postinstall": "npm run build:reactbits"
   },
   "dependencies": {
     "bcrypt": "^6.0.0",
+    "ogl": "^1.0.11",
     "pg": "^8.16.3",
     "postprocessing": "^6.36.0",
     "react": "^19.0.0",
diff --git a/public/style.css b/public/style.css
index 3dc49a4..e51aa8a 100644
--- a/public/style.css
+++ b/public/style.css
@@ -3548,18 +3548,43 @@ body.login-intro-active .app-container {
 }
 
 .agent-chat-shell {
+    position: relative;
     min-height: calc(100vh - 175px);
     display: flex;
     flex-direction: column;
     overflow: hidden;
+    isolation: isolate;
     border: 1px solid var(--card-border);
     border-radius: 24px;
-    background:
-        radial-gradient(circle at 80% 0%, rgba(var(--accent-rgb), 0.1), transparent 34%),
-        rgba(16, 18, 24, 0.72);
+    background: #000000;
     box-shadow: 0 24px 70px rgba(0, 0, 0, 0.24);
 }
 
+.agent-orb-background {
+    position: absolute;
+    inset: 0;
+    z-index: 0;
+    overflow: hidden;
+    pointer-events: none;
+}
+
+.agent-orb-background .orb-container,
+.agent-orb-background canvas {
+    width: 100%;
+    height: 100%;
+}
+
+.agent-orb-background canvas {
+    display: block;
+}
+
+.agent-chat-header,
+.agent-message-list,
+.agent-command-composer {
+    position: relative;
+    z-index: 1;
+}
+
 .agent-chat-header {
     min-height: 76px;
     display: flex;
@@ -3912,7 +3937,7 @@ body.login-intro-active .app-container {
 }
 
 html[data-theme="light"] .agent-chat-shell {
-    background: radial-gradient(circle at 80% 0%, rgba(var(--accent-rgb), 0.1), transparent 34%), rgba(255, 255, 255, 0.82);
+    background: #000000;
 }
 
 html[data-theme="light"] .agent-command-composer,
diff --git a/scripts/build-reactbits-orb.mjs b/scripts/build-reactbits-orb.mjs
new file mode 100644
index 0000000..a81942c
--- /dev/null
+++ b/scripts/build-reactbits-orb.mjs
@@ -0,0 +1,110 @@
+import { build } from 'esbuild';
+import { copyFile, mkdir, rm, writeFile } from 'node:fs/promises';
+import path from 'node:path';
+import { fileURLToPath } from 'node:url';
+
+const __dirname = path.dirname(fileURLToPath(import.meta.url));
+const projectRoot = path.resolve(__dirname, '..');
+const generatedRoot = path.join(projectRoot, '.reactbits', 'Orb');
+const publicRoot = path.join(projectRoot, 'public');
+
+// Official ReactBits implementation used by https://www.reactbits.dev/backgrounds/orb.
+const sourceBase = 'https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Orb';
+const sourceFiles = ['Orb.jsx', 'Orb.css'];
+
+async function downloadOfficialSource(fileName) {
+    const response = await fetch(`${sourceBase}/${fileName}`, {
+        headers: { 'user-agent': 'OutcomeAI ReactBits build' }
+    });
+
+    if (!response.ok) {
+        throw new Error(`Unable to download official ReactBits ${fileName}: HTTP ${response.status}`);
+    }
+
+    const source = await response.text();
+    if (source.length < 50) {
+        throw new Error(`Official ReactBits ${fileName} response was unexpectedly empty.`);
+    }
+    return source;
+}
+
+async function main() {
+    await rm(generatedRoot, { recursive: true, force: true });
+    await mkdir(generatedRoot, { recursive: true });
+    await mkdir(publicRoot, { recursive: true });
+
+    const downloaded = await Promise.all(
+        sourceFiles.map(async fileName => [fileName, await downloadOfficialSource(fileName)])
+    );
+
+    for (const [fileName, source] of downloaded) {
+        await writeFile(path.join(generatedRoot, fileName), source, 'utf8');
+    }
+
+    await copyFile(path.join(generatedRoot, 'Orb.css'), path.join(publicRoot, 'orb.css'));
+
+    const entryFile = path.join(generatedRoot, 'outcomeai-entry.jsx');
+    await writeFile(
+        entryFile,
+        `import React from 'react';\n` +
+            `import { createRoot } from 'react-dom/client';\n` +
+            `import Orb from './Orb.jsx';\n\n` +
+            `const mount = document.getElementById('outcomeAgentOrb');\n` +
+            `const agentView = mount?.closest('[data-view="agent"]');\n` +
+            `const shell = mount?.closest('.agent-chat-shell');\n` +
+            `let resizeObserver;\n\n` +
+            `function mountOfficialOrb() {\n` +
+            `  if (!mount || mount.dataset.reactBitsMounted === 'true') return;\n` +
+            `  if (agentView?.hidden || mount.clientWidth === 0 || mount.clientHeight === 0) return;\n` +
+            `  mount.dataset.reactBitsMounted = 'true';\n` +
+            `  createRoot(mount).render(React.createElement(Orb));\n` +
+            `  resizeObserver = new ResizeObserver(() => window.dispatchEvent(new Event('resize')));\n` +
+            `  resizeObserver.observe(mount);\n` +
+            `}\n\n` +
+            `function tryMountOfficialOrb() {\n` +
+            `  requestAnimationFrame(() => requestAnimationFrame(mountOfficialOrb));\n` +
+            `}\n\n` +
+            `if (agentView) {\n` +
+            `  new MutationObserver(tryMountOfficialOrb).observe(agentView, { attributes: true, attributeFilter: ['hidden'] });\n` +
+            `}\n` +
+            `if (document.readyState === 'loading') {\n` +
+            `  document.addEventListener('DOMContentLoaded', tryMountOfficialOrb, { once: true });\n` +
+            `} else {\n` +
+            `  tryMountOfficialOrb();\n` +
+            `}\n\n` +
+            `if (mount && shell) {\n` +
+            `  shell.addEventListener('pointermove', event => {\n` +
+            `    mount.dispatchEvent(new MouseEvent('mousemove', {\n` +
+            `      clientX: event.clientX,\n` +
+            `      clientY: event.clientY,\n` +
+            `      bubbles: false\n` +
+            `    }));\n` +
+            `  });\n` +
+            `  shell.addEventListener('pointerleave', () => {\n` +
+            `    mount.dispatchEvent(new MouseEvent('mouseleave', { bubbles: false }));\n` +
+            `  });\n` +
+            `}\n`,
+        'utf8'
+    );
+
+    await build({
+        entryPoints: [entryFile],
+        outfile: path.join(publicRoot, 'orb.js'),
+        bundle: true,
+        format: 'iife',
+        platform: 'browser',
+        target: ['es2020'],
+        jsx: 'automatic',
+        minify: true,
+        sourcemap: false,
+        legalComments: 'eof',
+        logLevel: 'info'
+    });
+
+    console.log('Built the official ReactBits Orb component with its unchanged default design.');
+}
+
+main().catch(error => {
+    console.error(error instanceof Error ? error.message : error);
+    process.exitCode = 1;
+});
diff --git a/test/dashboard-view.test.js b/test/dashboard-view.test.js
index e710f31..4f56e76 100644
--- a/test/dashboard-view.test.js
+++ b/test/dashboard-view.test.js
@@ -132,3 +132,12 @@ test('hub hero mounts the official ReactBits Hyperspeed preset-three component',
     assert.match(html, /src="\/hyperspeed\.js"/);
     assert.doesNotMatch(html, /class="hyperspeed-canvas"/);
 });
+
+
+test('AI agent mounts the official ReactBits Orb with unchanged defaults', () => {
+    const html = render();
+
+    assert.match(html, /id="outcomeAgentOrb"/);
+    assert.match(html, /href="\/orb\.css"/);
+    assert.match(html, /src="\/orb\.js"/);
+});
diff --git a/views/dashboard.js b/views/dashboard.js
index 478e202..33bc43b 100644
--- a/views/dashboard.js
+++ b/views/dashboard.js
@@ -25,6 +25,7 @@ function renderDashboardPage({ user, plans, billing, paymentConfiguration, aiCon
     <title>OutcomeAI - Workflow as a Service</title>
     <link rel="stylesheet" href="/style.css">
     <link rel="stylesheet" href="/hyperspeed.css">
+    <link rel="stylesheet" href="/orb.css">
     <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
 </head>
@@ -155,6 +156,7 @@ function renderDashboardPage({ user, plans, billing, paymentConfiguration, aiCon
 
             <section class="dashboard-view agent-view" data-view="agent" aria-label="AI Agent chat" hidden>
                 <div class="agent-chat-shell">
+                    <div class="agent-orb-background" id="outcomeAgentOrb" aria-hidden="true"></div>
                     <header class="agent-chat-header">
                         <div class="agent-chat-heading">
                             <span class="agent-status-dot" aria-hidden="true"></span>
@@ -692,6 +694,7 @@ function renderDashboardPage({ user, plans, billing, paymentConfiguration, aiCon
     </div>
 
     <script src="/hyperspeed.js" nonce="${escapeHtml(cspNonce)}" defer></script>
+    <script src="/orb.js" nonce="${escapeHtml(cspNonce)}" defer></script>
     <script src="/app.js" nonce="${escapeHtml(cspNonce)}" defer></script>
 </body>
 </html>`;
