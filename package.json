{
  "name": "outcomeai-login",
  "version": "3.0.0",
  "private": true,
  "description": "Node.js authentication with PostgreSQL, bcrypt, Google OAuth, and Discord OAuth.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check database.js && node --check public/app.js && node --check public/login.js && node --check public/register.js && node --check views/login.js && node --check views/register.js && node --check views/dashboard.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
