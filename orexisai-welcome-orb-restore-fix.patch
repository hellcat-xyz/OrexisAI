diff --git a/public/app.js b/public/app.js
index c027bbb..e279f97 100644
--- a/public/app.js
+++ b/public/app.js
@@ -585,7 +585,7 @@ function initializeAgentChat() {
     let deleteConfirmationTimer = null;
     let syncTimer = null;
     let uploadStatusTimer = null;
-    let welcomeOrbDismissed = false;
+    let welcomeOrbVisible = true;
     let welcomeTransitionTimer = null;
 
     historyList.addEventListener('click', (event) => {
@@ -661,9 +661,19 @@ function initializeAgentChat() {
     document.addEventListener('orexisai:welcome-orb-removed', finishWelcomeTransition);
     loadConversations();
 
+    function showWelcomeOrb() {
+        const orb = document.getElementById('outcomeAgentOrb');
+        if (welcomeOrbVisible && orb?.isConnected) return;
+
+        welcomeOrbVisible = true;
+        window.clearTimeout(welcomeTransitionTimer);
+        chatShell?.classList.remove('agent-conversation-entering', 'agent-conversation-active');
+        document.dispatchEvent(new CustomEvent('orexisai:show-welcome-orb'));
+    }
+
     function dismissWelcomeOrb(animate = true) {
-        if (welcomeOrbDismissed) return;
-        welcomeOrbDismissed = true;
+        if (!welcomeOrbVisible) return;
+        welcomeOrbVisible = false;
 
         const orb = document.getElementById('outcomeAgentOrb');
         const shouldAnimate = animate
@@ -1398,6 +1408,7 @@ function initializeAgentChat() {
         messageList.querySelectorAll('.agent-message, .agent-conversation-loading').forEach((node) => node.remove());
         emptyState.hidden = messages.length > 0;
         if (messages.length > 0) dismissWelcomeOrb(false);
+        else showWelcomeOrb();
         messages.forEach((message) => appendMessage(message, false));
         messageList.scrollTop = messageList.scrollHeight;
         document.getElementById('workspaceSearch')?.dispatchEvent(new Event('input'));
diff --git a/public/orb.css b/public/orb.css
index aec13e7..8b94178 100644
--- a/public/orb.css
+++ b/public/orb.css
@@ -13,7 +13,7 @@
     will-change: opacity, transform;
 }
 
-#outcomeAgentOrb.orb-container.orb-exiting {
+#outcomeAgentOrb.orb-exiting {
     opacity: 0;
     transform: translate3d(0, -16%, 0) scale(0.92);
     transition:
@@ -33,7 +33,7 @@
     transform-origin: center;
 }
 @media (prefers-reduced-motion: reduce) {
-    #outcomeAgentOrb.orb-container.orb-exiting {
+    #outcomeAgentOrb.orb-exiting {
         transition: none;
     }
 }
diff --git a/public/orb.js b/public/orb.js
index cf8db54..150f74c 100644
--- a/public/orb.js
+++ b/public/orb.js
@@ -1,15 +1,16 @@
 'use strict';
 
 (() => {
-    const mount = document.getElementById('outcomeAgentOrb');
-    if (!mount) return;
+    let mount = document.getElementById('outcomeAgentOrb');
+    const agentView = document.querySelector('[data-view="agent"]');
+    const shell = agentView?.querySelector('.agent-chat-shell');
+    if (!shell) return;
 
-    const agentView = mount.closest('[data-view="agent"]');
-    const shell = mount.closest('.agent-chat-shell');
     let cleanup = null;
     let mountQueued = false;
-    let removed = false;
     let exitTimer = 0;
+    let exitMount = null;
+    let exitTransitionHandler = null;
     let viewObserver = null;
 
     const vertexShaderSource = `
@@ -200,7 +201,7 @@
     }
 
     function initializeOrb() {
-        if (removed || cleanup || !mount.isConnected || mount.clientWidth === 0 || mount.clientHeight === 0) return;
+        if (!mount || cleanup || !mount.isConnected || mount.clientWidth === 0 || mount.clientHeight === 0) return;
 
         const canvas = document.createElement('canvas');
         canvas.setAttribute('aria-hidden', 'true');
@@ -348,49 +349,85 @@
     }
 
     function queueMount() {
-        if (removed || mountQueued) return;
+        if (!mount?.isConnected || mountQueued) return;
         mountQueued = true;
         requestAnimationFrame(() => {
             requestAnimationFrame(() => {
                 mountQueued = false;
-                if (!removed && (!agentView || !agentView.hidden)) initializeOrb();
+                if (mount?.isConnected && (!agentView || !agentView.hidden)) initializeOrb();
             });
         });
     }
 
-    function removeOrb() {
-        if (removed) return;
-        removed = true;
+    function cancelExit() {
         window.clearTimeout(exitTimer);
-        viewObserver?.disconnect();
-        document.removeEventListener('orexisai:dismiss-welcome-orb', handleDismissRequest);
+        exitTimer = 0;
+        if (exitMount && exitTransitionHandler) {
+            exitMount.removeEventListener('transitionend', exitTransitionHandler);
+        }
+        exitMount = null;
+        exitTransitionHandler = null;
+    }
+
+    function attachMount(nextMount) {
+        if (!nextMount?.isConnected) return;
+        cancelExit();
+        if (mount !== nextMount) {
+            cleanup?.();
+            mount = nextMount;
+        }
+        mount.classList.remove('orb-exiting');
+        delete mount.dataset.orbError;
+        queueMount();
+    }
+
+    function showOrb() {
+        let nextMount = document.getElementById('outcomeAgentOrb');
+        if (!nextMount) {
+            nextMount = document.createElement('div');
+            nextMount.id = 'outcomeAgentOrb';
+            nextMount.className = 'agent-orb-background';
+            nextMount.setAttribute('aria-hidden', 'true');
+            shell.insertBefore(nextMount, shell.firstChild);
+        }
+        attachMount(nextMount);
+    }
+
+    function removeOrb() {
+        if (!mount) {
+            document.dispatchEvent(new CustomEvent('orexisai:welcome-orb-removed'));
+            return;
+        }
+
+        const removedMount = mount;
+        cancelExit();
         cleanup?.();
-        mount.remove();
+        removedMount.remove();
+        if (mount === removedMount) mount = null;
         document.dispatchEvent(new CustomEvent('orexisai:welcome-orb-removed'));
     }
 
     function dismissOrb(animate) {
-        if (removed || mount.classList.contains('orb-exiting')) return;
+        if (!mount?.isConnected || mount.classList.contains('orb-exiting')) return;
         const shouldAnimate = animate
-            && !window.matchMedia('(prefers-reduced-motion: reduce)').matches
-            && mount.isConnected;
+            && !window.matchMedia('(prefers-reduced-motion: reduce)').matches;
 
         if (!shouldAnimate) {
             removeOrb();
             return;
         }
 
-        const finishOnTransition = (event) => {
-            if (event.target !== mount || event.propertyName !== 'transform') return;
-            mount.removeEventListener('transitionend', finishOnTransition);
-            removeOrb();
+        const activeMount = mount;
+        exitMount = activeMount;
+        exitTransitionHandler = (event) => {
+            if (event.target !== activeMount || event.propertyName !== 'transform') return;
+            if (mount === activeMount) removeOrb();
         };
 
-        mount.addEventListener('transitionend', finishOnTransition);
-        mount.classList.add('orb-exiting');
+        activeMount.addEventListener('transitionend', exitTransitionHandler);
+        activeMount.classList.add('orb-exiting');
         exitTimer = window.setTimeout(() => {
-            mount.removeEventListener('transitionend', finishOnTransition);
-            removeOrb();
+            if (mount === activeMount) removeOrb();
         }, 850);
     }
 
@@ -398,6 +435,7 @@
         dismissOrb(event.detail?.animate !== false);
     }
 
+    document.addEventListener('orexisai:show-welcome-orb', showOrb);
     document.addEventListener('orexisai:dismiss-welcome-orb', handleDismissRequest);
 
     if (agentView) {
@@ -409,8 +447,10 @@
     }
 
     if (document.readyState === 'loading') {
-        document.addEventListener('DOMContentLoaded', queueMount, { once: true });
-    } else {
-        queueMount();
+        document.addEventListener('DOMContentLoaded', () => {
+            if (mount) attachMount(mount);
+        }, { once: true });
+    } else if (mount) {
+        attachMount(mount);
     }
 })();
