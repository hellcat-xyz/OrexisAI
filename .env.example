PORT=3000
NODE_ENV=development
SESSION_SECRET=replace-this-with-a-long-random-secret

# Local development credentials. Change these before deploying.
AUTH_EMAIL=demo@outcomeai.local
AUTH_PASSWORD=OutcomeAI123!

# Recommended for production: replace AUTH_PASSWORD with a generated hash.
# Run: npm run hash-password -- "your strong password"
# AUTH_PASSWORD_HASH=scrypt$<salt>$<hex digest>
